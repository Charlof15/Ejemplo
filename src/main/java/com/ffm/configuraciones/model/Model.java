package com.ffm.configuraciones.model;

import lombok.Getter;
import lombok.Setter;

@Getter
@Setter
public class Model {
	/****
	 * Ejemplo de documentacion a nivel de modelos, normalemente aqui pongo lo que termina siendo parte 
	 * de la salida del WS
	 * 
	 * Ejemplo de etiquetas utilizadas en la documentacion del proyecto
	 * 
	 * @Schema(name = "id",description = "Identificador del catalogo de geografias", example = "3")
     * private Integer id;
	 * */
}
